if [ $#==1 ]
	then
		num=1234
		c=0
		while [ $num -gt 0 ]
			do
				ld=$(( $num%10 ))
				num= $(( $num/10 ))		
				c= $(( $c*10+$ld ))
		done
		echo $c;
	else
		echo "Exactly one number is to be entered";
fi



